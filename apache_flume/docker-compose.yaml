version: "3.8"

services:
  flume:
    build:
      context: .
    # Replace this with the agent you want to run
    command: "spool_to_console.conf spool_agent"
    # Used for spool directory tests
    volumes:
      - .flume_spool:/.flume_spool
